const User = require('./user');
const Listings = require('./listings');
const Reservation = require('./reservation');
const Review = require('./review');

module.exports = {User, Listings, Reservation, Review};